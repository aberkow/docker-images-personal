This image is built on top of the base `aberkow/node-8` image.

It will

- copy the contents of this directory into `/project`
- install dependencies from `package.json`
- run any script with `npm run start` 